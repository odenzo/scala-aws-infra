package com.odenzo.aws.keyspace

/** AWS Name for Cassandra now. The only thing we have to do is tag the keyspace
  * Keyspace API? Via CQL CREATE KEYSPACE mykeyspace WITH TAGS = {'key1':'val1', 'key2':'val2'} ;
  * and TABLES the same.
  */
object Keyspaces {

  def getEndpoint() = {}
}
